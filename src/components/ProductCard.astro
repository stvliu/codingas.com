---
interface Props { name: string; desc: string; href: string; }
const { name, desc, href } = Astro.props;
---
<a href={href} class="product-card">
  <h3>{name}</h3>
  <p>{desc}</p>
</a>

<style>
  .product-card { display: block; padding: 24px; border: 1px solid var(--sl-color-hairline-shade); border-radius: 12px; text-decoration: none; color: inherit; transition: box-shadow .2s; }
  .product-card:hover { box-shadow: 0 4px 12px rgba(0,0,0,.08); }
  .product-card h3 { margin: 0 0 8px; font-size: 1.2rem; }
  .product-card p { margin: 0; color: var(--sl-color-gray-3); font-size: 0.95rem; }
</style>
